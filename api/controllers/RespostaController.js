const database = require('../models')

class RespostaController {
    static async getAllRespostas(_, res) {
        try{

        } catch(error) {
            
        }
    }
}

module.exports = RespostaController